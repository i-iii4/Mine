# SPEC: Scroll edge fade

Related documents: [ARCHITECTURE.md](ARCHITECTURE.md) | [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md) | [PRINCIPLES.md](PRINCIPLES.md) | [SPEC_SETTINGS_WINDOW.md](SPEC_SETTINGS_WINDOW.md) | [SPEC_FRONTEND.md](SPEC_FRONTEND.md)

## Задача

Прокрученный контент упирается в верхний chrome жёстким срезом: строка обрывается
ровно по кромке. Опциональное растворение верхней кромки убирает этот срез —
контент истаивает в прозрачность, уходя под chrome.

Эффект применяется к четырём прокручиваемым поверхностям: боковая панель, лента,
развёрнутая карточка и результаты поиска.

## Растворение, не тень

Это **маска**, а не `box-shadow` и не тёмная накладка.

| Подход | Почему отвергнут |
|---|---|
| Тёмный градиент поверх контента | В светлой теме читается как грязное пятно, в тёмной почти не виден. Требует отдельных значений на тему |
| `box-shadow` у кромки | `box-shadow` в Mine зарезервирован за всплывающими элементами; разделение планов делается уровнями фона, границами и масками (см. `DESIGN_SYSTEM.md`) |
| Накладка градиентом цвета фона | Дёшево, но требует знать точный цвет под каждой поверхностью и разваливается на неоднородном фоне |
| **Маска прозрачности** (выбрано) | Контент действительно становится прозрачным, поэтому эффект не зависит ни от темы, ни от того, что лежит под поверхностью |

Кривая — та же, что уже используется в боковой панели для правой кромки строк и
превью. Одна кривая на все кромки: `src/lib/edgeFade.ts`.

## Две кромки — две задачи

Правая и верхняя кромки решают разное и потому используют разные кривые.

**Правая кромка** прячет переполнение текста в строках и превью боковой панели.
Она уходит в полную прозрачность и остаётся ручной таблицей из девяти пар
`alpha/progress` при ширине `EDGE_FADE_WIDTH = 24`.

**Верхняя кромка** создаёт впечатление контента, продолжающегося за границей.
Она генерируется формулой, сохраняет слабый остаток и масштабирует рампу под
контент, который закрывает.

## Контракт верхней кромки

### Замысел

Эффект — намёк на объём, а не растворение. Каждый пиксель, который задевает
рампа, — это ухудшенный контент: на светлом фоне тёмная картинка выбеливается и
читается как испорченная, а не как удалённая. Поэтому рампа короткая, а её пол
высокий: контент под chrome теряет около трети непрозрачности, не больше.

### Кривая

```
alpha(t) = minAlpha + (1 − minAlpha) · smootherstep(t)
smootherstep(t) = 6t⁵ − 15t⁴ + 10t³
```

`t = 0` — сама кромка, `t = 1` — конец рампы.

`smootherstep` выбран потому, что его первая и вторая производные равны нулю на
обоих концах. Это важно на каждом конце рампы: кривая, приходящая к плоскому
значению с ненулевым наклоном, даёт полосу Маха — зрение усиливает разрыв
производной и показывает светлую линию, которой нет в пикселях.

Гамма-коррекции здесь намеренно нет. Гамма имеет значение, когда рампа проходит
большую часть диапазона альфы, где воспринимаемая яркость резко расходится со
значением альфы. Эта рампа живёт в узкой полосе у полной непрозрачности, и
коррекция только утягивала бы кривую к прозрачному концу — именно от этого
контент выглядел выцветшим.

### Длина и плотность

Длина ограничена сверху ценой эффекта: рампа портит реальный контент, поэтому
остаётся заметно меньше одной строки боковой панели.

| Профиль | Поверхности | Длина | Непрозрачность у кромки |
|---|---|---|---|
| `TOP_FADE_CANVAS` | лента, развёрнутая карточка | 20px | 0.65 |
| `TOP_FADE_LIST` | боковая панель, поиск | 16px | 0.70 |

Списки получают более слабый эффект: текст строки несёт меньше визуальной массы,
чем фотография, поэтому та же альфа читается на нём сильнее.

Восприятие силы зависит от темы: в тёмной теме края изображений и так близки к
фону, в светлой любое осветление сразу видно как выцветание. Значения подобраны
по светлой теме как по худшему случаю.

Стопы генерируются из кривой: примерно один на `2.5px`, минимум 12, максимум 24.
Видимый артефакт — не 8-битная сетка, а линейная интерполяция *между* стопами:
при их нехватке рампа читается как набор плоских граней.

### Остаток

Рампа не доходит не только до нуля, но и близко к нему. Полностью растворённый
контент читается как обрезанный, сильно ослабленный — как испорченный; задача же
в том, чтобы показать слой, уходящий под chrome. Правая кромка по-прежнему уходит
в ноль: там она скрывает переполнение текста, а не границу.

## Условие появления

Маска включается только когда поверхность действительно прокручена:
`scrollTop >= TOP_FADE_SCROLLED_THRESHOLD_PX` (`1`). В покое первая строка обязана
оставаться полностью непрозрачной.

Порог в один пиксель отсекает субпиксельные смещения, которые приходят при
инерционной прокрутке и ресайзе; меньше пикселя контент скрыть не может.

Выключенная настройка и покой дают `undefined` — элемент остаётся вовсе без
mask-свойств, а не с `mask-image: none`.

## Два пути активации

| Путь | Кто использует | Почему |
|---|---|---|
| `topFadeMaskStyleFor(enabled, scrollTop)` | Лента | Grid уже держит собственный `scrollTop` для виртуализации. Второй слушатель `scroll` в горячем пути ленты не нужен |
| `useTopFadeMask(ref, enabled)` | Боковая панель, развёрнутая карточка, поиск | Эти поверхности своё смещение не отслеживают |

Хук хранит одно булево состояние — «прокручена ли поверхность», — а не смещение,
поэтому обычная прокрутка не вызывает повторный рендер после включения маски.

**Подключение идёт через callback ref, а не через ref-объект.** Поиск живёт внутри
Radix `Dialog`, который не монтирует содержимое, пока окно закрыто: список
результатов появляется в более позднем рендере, чем сам компонент. Ref-объект на
момент выполнения эффекта был бы пуст, а эффект больше не перезапустился бы —
такая поверхность молча никогда не растворялась бы.

`useTopFadeMask(forwardRef, enabled)` возвращает `{ ref, style }`. Переданный
`forwardRef` продолжает заполняться тем же узлом, поэтому хук добавляется к
поверхности, не ломая то, что уже использует этот ref для фокуса или прокрутки.

## Поверхности

| Поверхность | Скроллпорт | Атрибут состояния |
|---|---|---|
| Боковая панель | `[data-sidebar-scroll]` | `data-sidebar-top-fade` |
| Лента | `[data-grid-scroll]` | `data-grid-top-fade` |
| Развёрнутая карточка | `[data-detail-scroll]` | `data-detail-top-fade` |
| Поиск | `#search-overlay-listbox` | `data-search-results-top-fade` |

Атрибут присутствует только когда маска активна, и предназначен для проверок.

В ленте маска добавляется к существующему `style` через spread, а не вместо него:
скроллпорт держит собственные `padding`, `scrollbarGutter` и `transition`.

## Настройка

Ключ: `mine.scrollEdgeFade`. Выключено по умолчанию — эффект необязательный, и
чистая установка сохраняет прежнюю жёсткую кромку.

Хранение и синхронизация — по общему контракту настроек: окно настроек пишет
`localStorage` и вызывает `broadcastSettingsChange(SCROLL_EDGE_FADE_STORAGE_KEY)`,
главное окно перечитывает ключ в обработчике `settings-changed` и передаёт
значение в четыре поверхности пропом `scrollEdgeFade`.

Строка в разделе Appearance: `Fade content under the chrome`.

## Проверки

- `src/lib/edgeFade.test.ts` — кривая: якоря на концах, плоские концы против
  полос Маха, монотонность, отсутствие полностью прозрачных стопов, читаемость
  за серединой рампы; плотность стопов и размер сегмента; соотношение профилей;
  правая кромка и влияние `clearTailWidth`; поведение порога и стабильность
  объекта стиля на вариант.
- `src/hooks/useTopFadeMask.test.ts` — включение и выключение по прокрутке, узел,
  появившийся в более позднем рендере, повторное подключение после закрытия и
  открытия, синхронизация переданного ref, уже прокрученная поверхность,
  субпиксельные смещения, выключенная настройка, переключение настройки на лету,
  отсутствие узла, снятие слушателя при размонтировании.
- `src/components/Grid.test.tsx` — лента без маски при выключенной настройке;
  при прокрутке маска появляется и не затирает собственные стили скроллпорта.
- `src/components/SearchOverlay.test.tsx` — список результатов без маски в покое,
  растворение после прокрутки внутри открытого диалога, отсутствие эффекта при
  выключенной настройке.
- `src/settings/AppearanceSection.test.tsx` — значение по умолчанию, запись
  ключа, широковещание, восстановление сохранённого значения.
