# Онбординг: от установки до первой ценности

Related documents: [PRINCIPLES.md](PRINCIPLES.md) | [ARCHITECTURE.md](ARCHITECTURE.md) | [PLAN.md](PLAN.md) | [SPEC_CLIPPER.md](SPEC_CLIPPER.md) | [SPEC_VAULT_LIFECYCLE.md](SPEC_VAULT_LIFECYCLE.md) | [SPEC_CLOUD_STORAGE.md](SPEC_CLOUD_STORAGE.md) | [AUDIT_ACTIVATION.md](AUDIT_ACTIVATION.md) | [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)

Основание: [AUDIT_ACTIVATION.md](AUDIT_ACTIVATION.md). Решения приняты в
рабочей сессии 13–14.08.2026, зафиксированы здесь как требования.

## Цель

Довести человека от «скачал» до «вижу в ленте свои карточки», не требуя ни
терминала, ни чтения инструкций. Каждое требование ниже описывает наблюдаемое
поведение, а не реализацию.

Язык интерфейса — английский, включая все новые тексты онбординга.

## Точка входа — расширение, а не приложение

Расширение публикуется в Chrome Web Store **открыто**, доступно поиском.
Это меняет порядок знакомства: человек чаще ставит расширение первым и только
потом узнаёт про приложение. Расширение обязано быть самостоятельным.

**О1.** Расширение сохраняет клипы в папку на диске **без установленного
приложения**. Выбор папки запускается из самого расширения; выданное
разрешение на папку сохраняется между перезапусками браузера. Промежуточное
хранение внутри браузера не используется: файл должен лечь на диск сразу, как
это делают клипперы других локальных продуктов.

**О2.** Расширение знает, установлено приложение или нет, и ведёт себя
соответственно. Признак — наличие ответа от связующего звена, которое
приезжает вместе с приложением.

**О3.** В меню расширения (под многоточием) всегда доступны два действия:
`Open app` — когда приложение установлено, и `Download app` — когда нет.
Первое открывает приложение на текущем пространстве, второе ведёт на страницу
загрузки.

**О4.** Когда приложение установлено, сохранение идёт через него: помимо файлов
создаются превью и лента обновляется мгновенно. Когда не установлено — файлы
пишутся напрямую, а приложение при первом запуске подхватывает их обычным
сканированием папки. Пользователь разницы не замечает, кроме скорости
появления превью.

**О5.** Установка связующего звена не требует терминала.

Сделано 15.08.2026. Раздел `Clipper` в настройках: кнопка открывает страницу
расширения, вторая кнопка устанавливает связующее звено. Приложение копирует
его из собственного бандла — Tauri кладёт бинарник рядом с исполняемым файлом,
поэтому сборка ничего не компилирует на машине пользователя. Манифест пишется
для каждого обнаруженного браузера (Chrome, Dia, Arc, Edge, Brave);
ненайденные пропускаются молча. Идентификатор расширения по-прежнему вводится
вручную — до публикации в сторе он неизвестен заранее.

**О6.** Приложение сверяет версию установленного связующего звена со своей.

Сделано 15.08.2026. Рядом с установленным бинарником лежит отметка версии;
статус в настройках показывает расхождение и предлагает переустановить.

**О7.** В настройках виден статус связи.

Сделано 15.08.2026. Показывается: установлено ли звено, совпадает ли версия, в
каких браузерах зарегистрировано и какие браузеры вообще найдены на машине.

Разрешения расширения остаются в текущем виде: переход на внедрение по клику
не делаем.

## Утилита для видео

**О8.** `yt-dlp` поставляется внутри приложения.

Сделано 15.08.2026. Бинарник скачивается при сборке (закреплённая версия) и
попадает в бандл ресурсом; при установке связующего звена копируется рядом с
ним, потому что браузер запускает звено из каталога пользователя. Поиск
утилиты начинается с собственного каталога и только потом идёт по системным
путям, так что явная установка через менеджер пакетов по-прежнему побеждает.

## Первый запуск приложения

**О9.** Первый экран объясняет модель продукта тремя короткими тезисами.

Сделано 15.08.2026.

**О10.** Два явных действия вместо одного: `Create new space` создаёт папку
сама, предлагая расположение по умолчанию; `Open existing folder` открывает
системный выбор. Человек, который просто хочет попробовать, не должен
придумывать и создавать папку сам.

**О11.** Перед системным запросом доступа к папке — одна строка о том, что
сейчас спросит macOS и зачем.

Сделано 15.08.2026 в части предупреждения. Отдельный экран отказа в доступе не
сделан: провал чтения папки сейчас неотличим от её отсутствия и попадает в
состояние `Space unavailable`.

**О12.** Если выбранная папка не пуста, показывается подтверждение с фактами
до начала работы.

Сделано 15.08.2026. Подсчёт идёт без изменения файлов и ограничен по глубине,
чтобы само подтверждение не тормозило. Пустая папка открывается сразу.

**О13.** Первичная индексация показывает прогресс с числами обработанного, а
не бесконечный индикатор. Пользователь видит, что работа идёт и сколько
осталось.

## Пустая лента

**О14.** Пустое пространство никогда не выглядит просто пустым.

Сделано 15.08.2026, уточнено 16.08.2026. Два пути наполнения: установка
клиппера (ведёт в настройки) и перетаскивание файлов; третий путь — импорт из
Are.na — отменён вместе с импортом. Состояние показывается только на маршруте
Everything: пустая коллекция — это фильтр, который ничего не нашёл, и у неё
свой текст.

**О15.** Состояние исчезает с появлением первой карточки и больше не
возвращается.

**О16.** Пока клиппер не установлен, точка входа `Set up clipper` остаётся в
настройках — для второго браузера или второй машины.

## Импорт из Are.na

**О17.** Отменено 16.08.2026: импорт Are.na не входит в эту версию, без
обещаний на будущее. Вход из онбординга убран, код диалога спит без точки
входа — ровно так он жил до 15.08.2026.

**О18.** Снято 16.08.2026 вместе с отменой импорта (О17).
## Момент ценности

**О19.** После первой в пространстве сохранённой карточки — одноразовая
пометка о том, что карточка лежит файлом в выбранной папке, с действием
`Reveal in Finder`. Продукт продаёт локальность; показать её нужно в момент,
когда человек только что получил результат.

## Чего не делаем

- Не публикуем расширение как unlisted — только открыто.
- Не переводим интерфейс на русский.
- Не переходим на внедрение скриптов по клику.
- Не включаем Safari в первую раскатку.
